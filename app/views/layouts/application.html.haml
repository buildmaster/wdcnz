!!! 5
-# http://paulirish.com/2008/conditional-stylesheets-vs-css-hacks-answer-neither
-ie_html :class => 'no-js' do
  = render :partial => 'layouts/head'
  %body{ :lang => 'en', :class => "#{controller.controller_name}" }
    #container
      %header#header
        = render :partial => 'layouts/header'
      #main{ :role => 'main' }
        #sidebar-right{:role => 'menu'}
          = render :partial=> 'layouts/sidebar'
        #content
          = render :partial => 'layouts/flashes'
          = yield
       
      %footer#footer
        = render :partial => 'layouts/footer'
    #mask.mask.hide
      .winner
        .header
          .close
            %a{:href=>"#",:onclick=>"$('#mask').addClass('hide');return false;"}
              Close
          .title
            YOU WIN
        .message
          .line1
            You've unlocked a discount code. AWESOME!
          .line2
            For similar feelings of awesomeness, come to <span class="bolded">WDCNZ</span> on Thursday.
          %h2
            %a{:href=>"https://wdcnz.lilregie.com/voucher/4610249ff1 "}
              GET TICKETS
    -# Javascript at the bottom for fast page loading
    = render :partial => 'layouts/javascripts'
    